# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Codex local configuration (root only — never hide nested adapter dirs)
/.codex/
# Claude Flow runtime data
# Environment variables

# Local runtime artifacts
ruvector.db
.swarm/
node_modules/
office/.lock
*.tmp

# VCNP office runtime data — llm_batch writes full prompts/responses
# (jobs.json, failed.jsonl, .cache/) and telemetry holds per-call usage;
# both stay local, the ledger events.log.jsonl remains tracked.
office/batches/
office/telemetry.jsonl

# Telephone exchange «تلفنخانه» — real recorded voice (browser widget, CLI,
# and the real-PSTN voip-inbox-poll.js intake) stays LOCAL ONLY. This repo's
# GitHub remote is public; committing actual caller audio/voicemail would
# publish real people's voices.
office/phone/
office/.voip-inbox-cursor.json
