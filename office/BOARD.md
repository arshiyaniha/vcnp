# VCNP Office Board

> Human-readable kanban mirror of [`office/state.json`](state.json) — derived from the append-only event ledger [`office/events.log.jsonl`](events.log.jsonl), the single source of truth (plan §6). Regenerated by `report_generate`; works even if MCP is offline.

## Todo

_(empty)_

## Doing

_(empty)_

## Awaiting Orchestrator

_(empty)_

## Review

_(empty)_

## Blocked

_(empty)_

## Done

- **T-001** Landing hero section · executor · C2 · 100% · artifacts: 1
- **T-002** Features grid · executor · C2 · 100% · artifacts: 2
- **T-003** Contact footer · executor · C2 · 100% · artifacts: 1
- [x] **P0+P1 complete** — repository scaffolded per plan §13; envelope spec [`core/protocol.md`](../core/protocol.md) + constitution [`core/constitution.md`](../core/constitution.md) written; 9 role charters in [`core/charters/`](../core/charters/) wired into [`.roomodes.json`](../.roomodes.json). Project bootstrapped 2026-08-24.
- [x] **P2+P3 complete** — all 7 skills installed under [`skills/`](../skills/): core trio (`core-constitution`, `core-protocol` + envelope JSON Schema, `core-board-ops`) + capability skills (`web-design` + design-system starter CSS, `deploy-server` + per-recipe checklists, `security-basics`, `smart-resources`). Ledger event `phases_p2_p3_complete` appended 2026-08-24.
- [x] **P4 complete** — MCP server [`mcp/vcnp-office-mcp/`](../mcp/vcnp-office-mcp/) implemented: 13 tools (board CRUD with envelope-schema validation, compaction-freshness assignment gate, cost-truth ledger/telemetry, model router, async `llm_batch` with semantic cache, reports, compaction ack); Node ≥20, ZERO npm dependencies; registered as `vcnp-office` in [`.mcp.json`](../.mcp.json); smoke test **22/22 PASS** (`npm test`). Ledger event `phase_p4_complete` appended 2026-08-24.
- [x] **P5 complete** — demo golden path E2E (demo/run-golden-path.js: idea -> tasks -> build -> QA -> done; T-001..T-003 all Done) + Wall MVP dashboard (office/dashboard.html + templates/dashboard.html + office/dashboard-data.js) + bilingual docs (docs/README.md). Ledger event phase_p5_complete appended 2026-08-24.
- [x] **P6 complete** — Living Office Wall, pixel edition: [`templates/dashboard-pixel.html`](../templates/dashboard-pixel.html) + [`office/dashboard.html`](dashboard.html) (640×360 CSS pixel scene, 9 signal-driven avatars, meeting room with live-ticker wall screen, energy bars, day/night tint, collapsible status sidebar; zero dependencies, file:// ready). Ledger event `phase_p6_complete` appended 2026-08-24.
- [x] **P7+P8 complete (final build phases)** — one-click installers: [`installer/install.ps1`](../installer/install.ps1) + [`installer/install.sh`](../installer/install.sh) (Node ≥20 check with bilingual fail message, non-destructive `office/` scaffold, `.mcp.json` read→parse→merge→write preserving existing entries with ABSOLUTE server path, dashboard fallback copy, honest no-CLI-flag note) and [`installer/uninstall.ps1`](../installer/uninstall.ps1) + [`installer/uninstall.sh`](../installer/uninstall.sh) (`office/` kept unless `-DeleteOffice` / `--delete-office`). Verified against temp folders: install 20/20 PASS, uninstall-without-flag 5/5 PASS (pre-seeded decoy entry preserved), delete-flag path PASS — both PowerShell and Git Bash. Persian-first beginner manual [`docs/RAHNAMA-FA.md`](RAHNAMA-FA.md) linked as primary start from [`docs/README.md`](README.md). Ledger event `phase_p7_p8_complete` appended 2026-08-24.
